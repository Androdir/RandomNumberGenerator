import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	container: {
		backgroundColor: "#7126ea",
		borderRadius: 15,
	},
	button: {
		padding: 10,
	},
	text: {
		color: "#dfdede",
		fontSize: 20,
		fontWeight: "500",
		textTransform: "uppercase",
	}
});

export default styles